services:
  web:
    build: .          
    container_name: water-meter-frontend
    ports:
      - "7002:80"     
    restart: always
    volumes:
      # 【核心】把刚才写的 nginx.conf 替换掉容器里的默认配置
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro